name: adCNN
channels:
 - pytorch
 - nvidia
 - defaults
dependencies:
  - python
  - pytorch
  - torchvision
  - torchmetrics
  - pytorch-cuda
  - tqdm
  - numpy
  - pandas
  - matplotlib
  - scikit-learn
  - pillow
  - rich
  - pip
  - pip:
      - nibabel
      - kaggle
      - wandb
      - gradio
